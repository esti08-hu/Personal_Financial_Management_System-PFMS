# Database Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=root
POSTGRES_DB=test_db

# JWT
JWT_SECRET=test_jwt_secret_key_for_testing_purposes_only
JWT_EXPIRATION_TIME=3600

# Google Auth (test values)
GOOGLE_AUTH_CLIENT_ID=test_client_id
GOOGLE_AUTH_CLIENT_SECRET=test_client_secret

# Email
EMAIL_HOST=smtp.test.com
EMAIL_PORT=587
EMAIL_USER=test@example.com
EMAIL_PASSWORD=test_password

# Gemini AI (test values - will cause API errors but not module errors)
GEMINI_API_KEY=test_api_key

# Redis (test values)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=test_password